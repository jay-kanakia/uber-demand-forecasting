from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A completed end to end project deployed on aws - Predicting the demand for the cabs in the nyc region helping driver and company to maximize revenue and reduce idle time.',
    author='Jay Kanakia',
    license='',
)
